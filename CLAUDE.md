# Claude Instructions

## Python Libraries

Whenever you need to install a Python library:
1. Check if the working directory has a virtual environment
2. If yes: activate it, then install
3. If no: create a virtual environment using `uv`, activate it, then install any necessary libraries
